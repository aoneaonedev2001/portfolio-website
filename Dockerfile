# ใช้ Node.js เวอร์ชัน 18
FROM node:18

# ตั้งค่าไดเรกทอรีทำงาน
WORKDIR /app

# คัดลอกไฟล์ทั้งหมดไปยังไดเรกทอรีทำงาน
COPY . /app

# สร้างแอปพลิเคชัน
RUN npm ci

# เปิดพอร์ต 3000
EXPOSE 3000

# เริ่มต้นแอปพลิเคชัน
CMD ["npx", "serve","dist"]
